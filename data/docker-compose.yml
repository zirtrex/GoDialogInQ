version: '2'
services:
  mysql:
    image: 'mysql'
    restart: always
    volumes:
      - './mysql_data:/var/lib/mysql'
    environment:
      - MYSQL_ROOT_PASSWORD=godialoginq
    ports:
      - '3306:3306'